import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
// we have to import this because it is normally imported with layout, but that doesn't happen in this story
import "../../styles/style.scss"
import Navbar from './Navbar';
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs"

<Meta title="layout/Navbar" component={Navbar} decorators={[withKnobs]} />

# Navbar
The site navbar is rendered as part of every page that uses `Layout`.

## Props
<Props of={Navbar} />

## Demo
### Basic

<Preview>
    <Story name="basic">
        <Navbar pageName={text("Active Page Path", "")} />
    </Story>
</Preview>

### Admin

<Preview>
    <Story name="admin">
        <Navbar pageName={text("Active Page Path", "")} admin />
    </Story>
</Preview>

